var isPalindrome = (s)=>{
    let checkString = s.toLowerCase().replace(/[^a-z0-9]/g,' ');
    let left = 0;

    let right = checkString.length-1;
    while (left<right){
        if (checkString[left]!=checkString[right]){
            
        }
    }
}